'use strict';

const abcd = null;
let something;
console.log(something, null);
// what is difference between null and undefined?
// null means empty place
// undefined means literal "NOT DECLARED" = there's not value in it
// but they mean false though
